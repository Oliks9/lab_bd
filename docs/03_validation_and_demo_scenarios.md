# Проверка и сценарии демонстрации

Дата актуализации: 20.09.2026

## Минимум два правильных ответа от 20.09.2026

Успешны все 43 Python-теста, девять SQL smoke-тестов, проверка seed-данных и
повторное обновление пакета в отдельной схеме Oracle Free 23c с пятью правами.
В редакторе проверены `1`, `1,1`, `2,02`, исправление старого вопроса и правильный
набор `1,3`. В Oracle проверены отказ итоговой проверки и публикации при нуле/одном
правильном варианте, успешная публикация двух правильных, оценка одного выбранного
варианта как 0% и полного набора как 100%. Реальный драйвер подтвердил полный откат
ошибочного создания и редактирования, включая исходный тип и ID вариантов.
EXE пересобран: самопроверка зависимостей и проверка подключения успешны.
Временная схема удалена после проверки; рабочая база не изменялась.

Перед новым EXE требуется повторно выполнить актуальный `upgrade_question_options.sql`:
теперь он обновляет и спецификацию пакета с новой процедурой, и тело, без изменения данных.

## Правильные варианты от 20.09.2026

- Все 40 Python-тестов прошли, включая создание и редактирование одиночного вопроса,
  отсутствие выбора, старые несколько правильных, удаление выбранной строки,
  неверные номера множественного выбора и откат ошибки API.
- В новой временной схеме Oracle Free 23c с пятью правами без CREATE VIEW успешны
  установка, проверка seed-данных, все девять SQL smoke-тестов и двойной запуск
  `upgrade_question_options.sql`. Рабочая схема не переустанавливалась.
- Реальный OracleGateway: ошибочное создание не оставляет вопрос; ошибка редактирования
  сохраняет прежний текст и все ID вариантов благодаря ROLLBACK.
- Два параллельных соединения пытались добавить по правильному ответу одиночного
  вопроса: один вызов успешен, второй отклонён с `-20142`, в базе остался один ответ.
- Новый EXE пересобран, самопроверка зависимостей успешна; проверка подключения к
  тестовой Oracle-схеме завершилась с кодом 0. Временная схема после проверки удалена.

Ручная проверка: создайте «Один вариант» с тремя строками и выберите правильный
из выпадающего списка. Сохраните, откройте повторно и проверьте выбор. Переключите
на «Несколько вариантов»: доступны номера через запятую; `1,3` допустимо, `1,4`
для трёх строк недопустимо. Снова выберите одиночный тип: множественное поле скрыто,
сохраняется только один ответ из списка. Варианты другого типа не удаляются до сохранения.

## Проверка без CREATE VIEW от 19.09.2026

Изолированная схема на Oracle Free 23c: только `CREATE SESSION`, `CREATE TABLE`,
`CREATE SEQUENCE`, `CREATE PROCEDURE`, `CREATE TRIGGER`, квота 300 МБ.
CREATE VIEW ожидаемо отклонён с ORA-01031. Рабочая схема не переустанавливалась.

- Обе установки, `sql/install.sql` и `docker/install_modules.sql`, успешны;
  три пакета VALID, собственных представлений 0, данные seed сохранены.
- Восемь SQL smoke-тестов и отдельный `seed_installation_test.sql` прошли.
  Новый `reports_smoke_test.sql` проверяет фильтры каталога и права аналитики.
- Двойной запуск `upgrade_no_views.sql` проверен сравнением всех строк всех
  15 таблиц до и после, включая хеши паролей: данные не изменились.
- Реальные вызовы OracleGateway проверили 7 процедур, 24 результата и 144 ответа,
  пустые выборки, тематический фильтр, историю, аналитику и рейтинг.
- 32 Python-теста прошли, включая пять новых тестов передачи параметров,
  чтения/закрытия курсоров, обработки ошибок и отсутствия COMMIT при чтении.
- С реальной БД открыты каталог, история, результаты и студия для трёх ролей.
  Пересобранный EXE прошёл `--self-check` и `--connection-check` (код завершения 0).

Порядок обновления учебного сервера: `10_deployment_without_views.md`.
Ниже сохранены также результаты прежних проверок с их датами.

## 1. Цель проверок

Подбор N по категории и сложности проверяется файлом `sql/tests/question_selection_smoke_test.sql`.
Сценарий GUI и пошаговое обновление схемы приведены в `07_question_selection.md`.

Результат серверной проверки 12.09.2026: на момент прогона Docker был запущен, контейнер Oracle имел статус `healthy`.
Миграция подбора применена, повторный запуск `docker/upgrade.ps1` также успешен.
Все объекты Oracle имеют статус `VALID`, все семь SQL smoke-тестов и шесть Python-тестов
(GUI и транзакционная обёртка) проходят. Проверена вкладка «Подбор» с реальной базой
при размере окна 1060x700. Старые SQL-проверки теперь создают собственные временные тесты,
поэтому не зависят от наличия или изменения демонстрационного теста. Временные данные
откатываются и при успехе, и при ошибке; уникальные имена исключают конфликты с рабочими данными.

Подтвердить, что:

- Oracle-объекты корректно установлены и валидны;
- серверная бизнес-логика работает независимо от GUI;
- ключевые сценарии (авторинг, публикация, прохождение, удаление, администрирование) выполняются без потери консистентности.

## 2. Подготовка окружения

1. Поднять БД и приложение:
   - `powershell -ExecutionPolicy Bypass -File .\docker\start.ps1`
2. При необходимости обновить схему:
   - `powershell -ExecutionPolicy Bypass -File .\docker\upgrade.ps1`
3. Прогнать SQL smoke-тесты:
   - `powershell -ExecutionPolicy Bypass -File .\docker\check.ps1`

## 3. Автоматические SQL-проверки

| Скрипт | Что проверяет | Критерий успеха |
|---|---|---|
| `sql/tests/check_invalid_objects.sql` | Валидность функций, процедур, пакетов, триггеров | Нет `INVALID`, блок завершился без `RAISE` |
| `sql/tests/auth_smoke_test.sql` | Регистрация, вход, создание автора | Возврат корректных ролей `USER`/`AUTHOR` |
| `sql/tests/smoke_test.sql` | Сквозное прохождение временного теста с шестью типами вопросов | Итог `score_percent = 100` |
| `sql/tests/content_management_smoke_test.sql` | Создание/редактирование/публикация/удаление и сбросы | Все контрольные условия выполнены |
| `sql/tests/timezone_smoke_test.sql` | Логика с `TIMESTAMP WITH TIME ZONE` | Попытка остается `IN_PROGRESS` в `+03:00` сессии |

## 4. Матрица ручной проверки ключевой логики

### Каталог: нижняя панель и масштабирование

Выполните `python -m unittest discover -s app/tests -v` с `app` в `PYTHONPATH` и рабочим Tk.
Для среды проекта с bundled-runtime используйте настройки из `build/build_exe.ps1`.
При исправлении каталога прошли девять Python-тестов, включая три проверки
`test_catalog_layout.py`. После добавления диагностики зависимостей всего 15 тестов;
актуальная проверка сборки описана в разделе 10.

1. Откройте каталог при 1060x700, 1240x820 и 1920x1080. Кнопка «Начать выбранный тест» должна быть полностью видна и доступна, строки списка также должны оставаться видимыми.
2. Проверьте с повышенным масштабом Windows. Автотесты меняют масштаб Tk примерно от 100% до 200%; ручная проверка на целевом мониторе дополнительно проверяет масштабирование всей ОС.
3. Выберите тест с длинным названием/описанием или подбором вопросов. Вся информация доступна через полосу прокрутки и колесо мыши внутри карточки, кнопка запуска не прокручивается вместе с ней.
4. Выберите пустую тематику: кнопка запуска отключается. Вернитесь ко всем тематикам: выбранный тест снова можно запустить кнопкой или Enter.

Исправление меняет только GUI: для другого компьютера достаточно нового EXE, данные Oracle переустанавливать не нужно.

| Сценарий | Шаги | Ожидаемый результат |
|---|---|---|
| Регистрация пользователя | Заполнить форму регистрации и войти | Успешный вход с ролью `USER` |
| Создание автора | Войти под `admin`, создать автора в разделе «Команда» | Новый пользователь получает роль `AUTHOR` |
| Создание тематики/категории | Войти под `AUTHOR`, добавить тему и категорию | Новые записи сразу доступны в UI без повторного входа |
| Создание черновика теста | Автор создает тест и задает `timer_mode`, `attempt_limit`, `show_feedback` | Тест в `DRAFT`, настройки сохранены |
| Редактирование вопроса | Добавить вопрос, затем открыть и изменить поля | Изменения сохранены, старые options заменены при смене типа |
| Публикация теста | Нажать «Опубликовать» | Публикация проходит только при валидной структуре вопросов |
| Возврат в черновик | Для опубликованного теста нажать «Скрыть в черновик» | Статус снова `DRAFT`, доступно редактирование |
| Ограничение попыток | Установить `attempt_limit=1`, пройти тест 2 раза | Вторая попытка блокируется ошибкой лимита |
| Таймер `QUESTION` | Запустить тест с таймером на вопрос, дождаться таймаута | Происходит автопереход к следующему вопросу |
| Показ пояснений | Включать/выключать `show_feedback`, пройти тест | В результате пояснения показываются только при `show_feedback=1` |
| Удаление вопроса | В черновике удалить вопрос | Пересчитаны `seq_no`, статистика попыток и проценты |
| Удаление теста | Удалить тест автора | Тест удален, связанные попытки удалены |
| Сброс по тесту | Под `ADMIN` вызвать сброс попыток пользователя по тесту | У пользователя нет попыток по выбранному тесту |
| Полный сброс | Под `ADMIN` выполнить полный сброс прогресса | У пользователя отсутствуют все попытки |
| Смена пароля | Под `ADMIN` сменить пароль пользователю | Пользователь входит только с новым паролем |
| Отключение/включение аккаунта | Под `ADMIN` отключить пользователя, затем включить обратно | В отключенном состоянии вход запрещен, после включения вход снова доступен |
| Удаление пользователя | Под `ADMIN` удалить пользователя не `ADMIN` | Пользователь и его зависимые данные очищены |

## 5. Проверка по PL/SQL API (без GUI)

Рекомендуется демонстрировать преподавателю, что логика живет в БД:

1. `pr_register_user` / `pr_login`.
2. `pkg_admin.create_topic`, `create_category`, `create_quiz`.
3. `pkg_admin.add_question`, `add_option`, `publish_quiz`.
4. `pkg_testing.start_attempt`, `submit_answer`, `finish_attempt`.
5. `pkg_admin.delete_question` / `delete_quiz`.
6. `pkg_admin.reset_user_quiz_attempts` / `reset_user_progress`.

Если вызвать недопустимую операцию, Oracle должен вернуть `RAISE_APPLICATION_ERROR` с соответствующим кодом.

## 6. Контроль демонстрации на защите

Чек-лист:

- [ ] Показан вход `admin / Admin123!`.
- [ ] Показано создание автора и вход под ним.
- [ ] Показано создание темы, категории и теста.
- [ ] Показаны все ключевые типы вопросов (включая `ORDERING`).
- [ ] Показана публикация и возврат в черновик.
- [ ] Показан лимит попыток.
- [ ] Показан автопереход по таймауту вопроса.
- [ ] Показаны результаты с/без пояснений (`show_feedback`).
- [ ] Показано удаление вопроса и теста.
- [ ] Показаны админ-операции: сбросы, смена пароля, отключение/включение и удаление пользователя.
- [ ] Показан прогон SQL smoke-тестов.

## 7. Критерии «проект работает корректно»

- Все Oracle-объекты имеют статус `VALID`.
- Нет ошибок целостности после удаления вопросов/тестов/пользователей.
- Роли и права соблюдаются на стороне БД.
- Таймеры и оценивание совпадают с бизнес-правилами.
- EXE запускается и успешно подключается к Oracle.

## 8. Проверка распределения логики (09.09.2026)

Запустите `docker/check.ps1`: он включает `server_logic_smoke_test.sql` с прямыми
вызовами процедур. Проверяются отказ раннего таймаута, отказ позднего ответа,
единственная активная попытка, лимит прохождений, завершение последним ответом
и последним таймаутом, скрытие пояснений в `pkg_reports.attempt_details` и закрытие старой попытки через `pr_login`.
Тестовые данные откатываются, существующие результаты не удаляются.

В GUI проверьте текстовые кнопки на входе, в студии и при прохождении теста.
Подключение: `localhost:1521/FREEPDB1`, схема `quiz_app`, пароль `P@ssw0rd`.
Для уже созданных Docker-томов сначала запустите `docker/change_schema_password.ps1`.
Пароль `Admin123!` в сценариях относится только к новой установке: для существующего
аккаунта используйте ранее установленный пароль приложения.

## 9. Сравнение со средними (11.09.2026)

1. Создайте тест с пятью вопросами по одному баллу и разрешите повторное прохождение.
2. Пройдите тест участником А на 60%, участником Б на 40%.
3. Пройдите тот же тест участником В на 80%. В результате должны быть среднее 50%, разница +30 п. п., два участника и две попытки в сравнении.
4. Повторно пройдите тест участником В на 0%. Среднее для В останется 50%; разница новой попытки будет -50 п. п.
5. Проверьте случай одинакового результата со средним и случай без результатов других участников. Отсутствие выборки не должно отображаться как среднее 0%.
6. Сбросьте результаты А и Б через администратора и повторно откройте результат В. Должно появиться сообщение об отсутствии других результатов.

Незавершённые попытки и попытки без положительного максимального балла не участвуют
в среднем; прерванные попытки с оценкой участвуют. Собственные повторные прохождения
исключаются полностью. Подробности правил и округления: `02_database_logic_map.md`, раздел 11.6.

Автоматическая SQL-проверка: `sql/tests/comparison_smoke_test.sql` (F5 в SQL Developer).
На уже установленном учебном сервере сначала выполните `sql/upgrade_no_views.sql`,
затем проверку и замените EXE. Повторная полная установка не нужна.

## 10. Проверка сборки и зависимостей (13.09.2026)

Для этой проверки Oracle и Docker не требуются. Из корня проекта, в обычной
установке Python с Tcl/Tk, выполните:

```powershell
$env:PYTHONPATH = "$PWD\app"
.\.venv\Scripts\python.exe -m unittest discover -s app/tests -v
.\.venv\Scripts\python.exe -m compileall -q app
powershell -ExecutionPolicy Bypass -File .\build\build_exe.ps1 -Python .\.venv\Scripts\python.exe
```

Для локального bundled Python дополнительно нужны настройки `TCL_LIBRARY`,
`TK_LIBRARY` и пути к `_tkinter` из `build/build_exe.ps1`.

Прогон 15 Python-тестов успешен: каталог, подбор, транзакционная обёртка и шесть
новых проверок диагностики. Проверены сохранение причины `ImportError`, отдельная
ошибка криптографии, продолжение остальных проверок после ошибки, запись JSON,
работа без консольных потоков и обработка недоступного пути отчёта.

Готовый Windows EXE прошёл `--self-check` с кодом `0` и `frozen: true` вне песочницы:
`oracledb 4.0.1` в Thin mode, `cryptography 48.0.0`, Tcl/Tk `8.6.15`, Python `3.12.14 x64`.
В песочнице проверка Tk возвращала ошибку доступа к `init.tcl`; тот же файл вне неё
успешен. Это ограничение среды проверки, не доказательство отсутствия ресурсов в EXE.

Результат относится к локальному Windows 11. На целевом Windows Server выполните
диагностику повторно по `08_build_and_dependencies.md`, затем проверьте вход через GUI.
Проверка зависимостей не выполняет вход и не подтверждает доступность удалённой БД.
SQL в этом изменении не менялся; серверные тесты повторно не запускались.

## 11. Начальные тесты и аккаунты (18.09.2026)

Только сразу после новой установки выполните `sql/tests/seed_installation_test.sql`
через F5 в SQL Developer. В Docker init и переустановку эта проверка уже включена.
На рабочей базе с изменёнными паролями, удалёнными демо-тестами или добавленными
участниками её выполнять не нужно: она проверяет именно исходное состояние.

Ожидаемое начальное состояние:

| Объекты | Количество и состав |
| --- | --- |
| Аккаунты | 3: admin (ADMIN), author (AUTHOR), student (USER) |
| Тематики и категории | По 3, категории вопросов соответствуют тематике теста |
| Тесты | 3: Oracle, математика, русский язык; все опубликованы и публичны |
| Вопросы | 18: по 6 типов в каждом тесте, с пояснениями |
| Максимальный балл | По 8 на тест |
| Попытки | 24 завершённые: по 12 у author и student, по четыре каждого теста |
| Сохранённые ответы | 144, все шесть типов у обоих участников |

Проверка входит под всеми тремя начальными аккаунтами, проверяет роли и имена,
владение двумя предметными тестами автором, настройки доступа, таймера и пояснений.
Затем от имени участника проходит все три теста через `pkg_testing`, получает
`FINISHED` и 100% в каждом. Дополнительные проверочные попытки откатываются к savepoint,
в том числе при ошибке. Исходные 24 демонстрационные сессии сохраняются.

Фактически проверены `sql/install.sql` и `docker/install_modules.sql` в изолированной
временной схеме локального Oracle: оба маршрута создали ожидаемые данные, все объекты
VALID, все семь существующих SQL smoke-тестов успешны. После каждого прогона осталось
три аккаунта, три теста, 18 вопросов и 24 завершённые попытки. Сохранность результатов
проверена после переподключения, повторный запуск `04_demo_attempts.sql` не добавил
дубликатов. Проверены 144 ответа и четыре разных процента результатов. Временная схема удалена,
рабочая `quiz_app` не переустанавливалась. Для запуска обычного установщика из SQL*Plus
текущим каталогом был `sql`, чтобы корректно находились вложенные скрипты.

После удаления комментариев повторно прошли 15 Python-тестов и компиляция исходников.
Проверены Python-токены и AST (комментарии и docstring отсутствуют), PowerShell-токены
(комментарии отсутствуют) и SQL-файлы. SQL-строки Python и литералы цветов не удалялись.
EXE пересобран 18.09.2026; скрипт завершился сообщением `EXE verified`, все три
проверки зависимостей успешны. Начальные данные в EXE не упаковываются: они находятся в Oracle.

Для демонстрации вручную войдите как `student / Student123!`, откройте каталог и
раздел «Мои результаты»: уже должно быть 12 завершённых сессий. Можно также пройти
любой начальный тест самостоятельно. Затем войдите как `author / Author123!`: в студии
должны быть доступны собственные тесты по математике и русскому языку.
В его разделе «Мои результаты» также должно быть 12 сохранённых прохождений.
Запросы для подтверждения количества пользователей, сессий и типов ответов находятся
в README, раздел «Проверка условий допуска к защите».
Данные для входа относятся к новой установке, а не меняют пароли существующей базы.

## 12. Разметка вкладки «Команда» (18.09.2026)

Автоматические проверки: `app/tests/test_team_layout.py`. Прогон всего Python-набора
завершился успешно: 21 тест, включая шесть новых проверок вкладки «Команда».
Компиляция исходников через `compileall` также успешна.

Проверены размеры окна 1060×700, 1240×820, 1920×1080 и масштабы Tk 4/3, 5/3, 2, 8/3
(примерно 100%, 125%, 150%, 200% Windows). Проверка учитывает изменение размера уже
открытой вкладки: каждая кнопка полностью помещается в контейнеры, кнопки не
перекрываются, как минимум первая строка пользователей доступна. Дополнительно
проверены прокрутка 80 аккаунтов и широких колонок, пустой список и отсутствие выбора.

Формы создания автора и смены пароля проверяются на корректную передачу ID и
значений в gateway, валидацию, отображение ошибки и возможность повторного сохранения.
Проверены скрытие пароля, Enter, Escape, освобождение модального окна и сохранение
вкладки «Команда» после создания автора. Для роли, активности и удаления проверены
аргументы вызова, отмена подтверждения и возврат на ту же вкладку.

В этих тестах используется подставной gateway: реальные аккаунты и пароли не меняются.
SQL не изменялся; повторного прогона Oracle smoke-тестов для этой правки не было.
Автоматические проверки геометрии не заменяют визуальную проверку на целевом Windows Server.

Новый EXE собран `build/build_exe.ps1` и прошёл встроенную проверку зависимостей
(`EXE verified`, `ok: true`, `frozen: true`): драйвер Oracle в Thin mode,
cryptography и Tcl/Tk доступны. Самопроверка не подключается к рабочей базе.

Ручная проверка после замены EXE:

1. Войти администратором и открыть «Студия тестов» → «Команда».
2. Уменьшить окно: кнопки под таблицей должны остаться видимыми, при необходимости
   перейти на следующую строку. Список пользователей прокручивается независимо.
3. Выбрать тестовый аккаунт и нажать «Сменить пароль». Проверить логин в заголовке
   окна, затем закрыть окно кнопкой «Отмена», не меняя пароль.
4. Открыть «Добавить автора» и закрыть через Escape. Для проверки сохранения
   использовать только специально созданный тестовый аккаунт, не рабочий.
5. При необходимости проверить смену пароля и повторный вход тестового аккаунта.
   Переустановка схемы и повторное заполнение начальных данных не требуются.

## 13. Доступность остальных страниц (19.09.2026)

Добавлен `app/tests/test_pages_layout.py`, шесть новых проверок. Всего успешно
выполняются 27 Python-тестов. Проверки используют настоящий Tkinter и подставной
gateway: изменения реальных пользователей, тестов и попыток не выполняются.

- Все восемь разделов студии: размеры 1060×700, 1240×820, 1920×1080 и масштабы
  Tk 4/3, 5/3, 2, 8/3. Проверяется переключение через список разделов и доступность
  полей/кнопок после прокрутки; для аналитики проверяется загрузка детализации.
- Подключение, вход, регистрация: три масштаба, включая 200%, минимальное окно.
- История и результат: открытие при 100% и 200%, доступность таблицы и навигации.
- Прохождение: все шесть типов, длинный вопрос и двенадцать длинных вариантов,
  доступность кнопок и передача числового ответа в прежний gateway.
- Конструктор: переключение всех типов, выбор существующего вопроса, изменение
  подписи кнопки сохранения без обрезания и очистка формы.
- Переход фокуса к нижней кнопке регистрации и прокрутка колесом мыши.

Проверки «Команды» и «Подбора» теперь учитывают прокручиваемую вкладку: важна
доступность элемента, а не одновременное размещение всех элементов в окне.
Каталог по-прежнему проверяется на постоянно видимую кнопку запуска.

Ручная проверка целевого компьютера: изменить масштаб Windows, открыть нижнюю
часть регистрации, публикации и редактора вопросов, пройти длинный вопрос,
открыть результаты и переключить разделы студии через список. На очень широких
формах использовать нижнюю горизонтальную полосу. Таблицы прокручиваются отдельно
от страницы. Если выбранный элемент шире окна, его края доступны горизонтальной
прокруткой, а текст в поле ввода прокручивается штатно.

SQL не менялся и серверные smoke-тесты повторно не запускались. Геометрия проверена
автоматически локально; это не проверка реального DPI и подключения на удалённом
Windows Server. Перед демонстрацией проверьте новый EXE на целевом компьютере.

Финальный EXE пересобран с новым модулем разметки и защитой обработчика фокуса.
Сборщик завершился сообщением `EXE verified`; драйвер Oracle, cryptography
и Tcl/Tk прошли самопроверку. Компиляция Python и проверка отсутствия комментариев
и docstring в новом коде также успешны.
